import express from "express";import {getBlogs,getBlogBySlug,createBlog,updateBlog,deleteBlog} from "../controllers/blogController.js";import protect from "../middleware/authMiddleware.js";import allowRoles from "../middleware/roleMiddleware.js";const router=express.Router();router.get("/",getBlogs);router.get("/:slug",getBlogBySlug);router.post("/",protect,allowRoles("admin"),createBlog);router.patch("/:id",protect,allowRoles("admin"),updateBlog);router.delete("/:id",protect,allowRoles("admin"),deleteBlog);export default router;
