const sendOTP = async ({email,otp}) => {
  // Development implementation. Replace with your email/SMS provider in production.
  console.log(`[Purabi Insurance] OTP for ${email}: ${otp}`);
};
export default sendOTP;
