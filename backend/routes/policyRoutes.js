import express from "express";import {createPolicy,getPolicies,getPolicyById,updatePolicyStatus} from "../controllers/policyController.js";import protect from "../middleware/authMiddleware.js";import allowRoles from "../middleware/roleMiddleware.js";const router=express.Router();router.post("/",protect,allowRoles("agent","admin"),createPolicy);router.get("/",protect,getPolicies);router.get("/:id",protect,getPolicyById);router.patch("/:id/status",protect,allowRoles("agent","admin"),updatePolicyStatus);export default router;
