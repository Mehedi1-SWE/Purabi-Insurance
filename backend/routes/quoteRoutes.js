import express from "express";import {createQuote,getQuotes,updateQuoteStatus} from "../controllers/quoteController.js";import protect from "../middleware/authMiddleware.js";import allowRoles from "../middleware/roleMiddleware.js";const router=express.Router();router.post("/",createQuote);router.get("/",protect,getQuotes);router.patch("/:id/status",protect,allowRoles("agent","admin"),updateQuoteStatus);export default router;
