import express from "express";import {createClaim,getClaims,getClaimById,updateClaimStatus} from "../controllers/claimController.js";import protect from "../middleware/authMiddleware.js";import allowRoles from "../middleware/roleMiddleware.js";const router=express.Router();router.post("/",protect,createClaim);router.get("/",protect,getClaims);router.get("/:id",protect,getClaimById);router.patch("/:id/status",protect,allowRoles("agent","admin"),updateClaimStatus);export default router;
