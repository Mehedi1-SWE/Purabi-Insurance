import express from "express";import {createAgent,getAgents,getMyAgentProfile,updateAgent} from "../controllers/agentController.js";import protect from "../middleware/authMiddleware.js";import allowRoles from "../middleware/roleMiddleware.js";const router=express.Router();router.post("/",protect,allowRoles("admin"),createAgent);router.get("/",protect,allowRoles("admin"),getAgents);router.get("/me",protect,allowRoles("agent"),getMyAgentProfile);router.patch("/:userId",protect,allowRoles("admin","agent"),updateAgent);export default router;
